# ADMM Deconvolution with Diffusion Prior

This project can be run with:

      python main.py --{method}  --sigma_noise {noise level}

Methods include: nlm, TV, bilateral, dncnn, diffusion

If no method argument is provided, the default setting is to use all of them. 

### Example
To run diffusion on a noise level of 0.1:
    
      python main.py --diffusion  --sigma_noise 0.1
